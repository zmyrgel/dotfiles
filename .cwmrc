# Autogroup definitions
autogroup 3 "aterm,XTerm"
autogroup 3 "xterm,XTerm"
autogroup 1 "firefox,We"

# Set default Xft(3) font
#borderwidth 1
#color activeborder 
#color font
#color selfont
#color groupborder
#color inactiveborder
#color menubg
#color urgencyborder
#color ungroupborder

fontname "terminus:pixelsize=14:bold"
#gap top bottom left right

# Turn on sticky-group mode
#sticky yes

# Any entry here is shown in the application menu
command firefox		firefox
command emacs		"xterm -e emacs -nw"
command deadbeef	deadbeef
command top		"xterm -e top"

# Ignore programs by that name by not drawing borders around them.
ignore XMMS
ignore xwi
ignore xapm
ignore xclock

### Keybindings ###

bind CM-Return unbind
bind 4-Return terminal
bind CM-Delete unbind	# Lock the screen.
bind 4-l lock
#bind M-Return unbind	# Hide current window.
#bind M-Down unbind	# Lower current window.
bind M-Up unbind	# Raise current window.
bind M-slash unbind	# Search for windows.
bind 4-w search
bind C-slash unbind	# Search for applications.
bind 4-a menusearch
bind 4S-g groupsearch	# Search for groups. 
bind CM-n unbind	# Label current window.
bind 4-n label
#M-Tab           Cycle through currently visible windows.
#MS-Tab          Reverse cycle through currently visible windows.
bind CM-x unbind
bind 4-x delete
bind CM-1 unbind	# Select group n, where n is 1-9.
bind CM-2 unbind
bind CM-3 unbind
bind CM-4 unbind
bind CM-5 unbind
bind CM-6 unbind
bind CM-7 unbind
bind CM-8 unbind
bind CM-9 unbind
bind 4-1 group1
bind 4-2 group2
bind 4-3 group3
bind 4-4 group4
bind 4-5 group5
bind 4-6 group6
bind 4-7 group7
bind 4-8 group8
bind 4-9 group9
bind 4S-1 movetogroup1
bind 4S-2 movetogroup2
bind 4S-3 movetogroup3
bind 4S-4 movetogroup4
bind 4S-5 movetogroup5
bind 4S-6 movetogroup6
bind 4S-7 movetogroup7
bind 4S-8 movetogroup8
bind 4S-9 movetogroup9
bind 4C-1 grouponly1
bind 4C-2 grouponly2
bind 4C-3 grouponly3
bind 4C-4 grouponly4
bind 4C-5 grouponly5
bind 4C-6 grouponly6
bind 4C-7 grouponly7
bind 4C-8 grouponly8
bind 4C-9 grouponly9
bind CM-0 unbind	# Select all groups.
bind 4S-a nogroup
bind CM-g unbind	# Toggle group membership of current window.
bind 4-g grouptoggle
# bind M-Right cyclegroup	# Cycle through active groups.
# bind M-Left rcyclegroup	# Reverse cycle through active groups.
bind CMS-f unbind	# Toggle freezing geometry of current window.
bind 4M-f freeze
bind CM-s unbind	# Toggle stickiness of current window.
#bind 4-o sticky
bind CM-f unbind	# Toggle full-screen mode of current window.
bind 4-f fullscreen
bind CM-m unbind	# Toggle maximization of current window.
bind 4-m maximize
#bind CM-equal unbind	# Toggle vertical maximization of current window.
#bind CMS-equal unbind	# Toggle horizontal maximization of current window.
bind C-Up unbind
bind C-Down unbind
bind C-Left unbind
bind C-Right unbind
bind CS-Up unbind
bind CS-Down unbind
bind CS-Left unbind
bind CS-Right unbind
bind M-h unbind		# Move window by a small amount.
bind M-j unbind
bind M-k unbind
bind M-l unbind
bind MS-h unbind		# Move window by a large amount; see cwmrc(5).
bind MS-j unbind
bind MS-k unbind
bind MS-l unbind
bind CM-h unbind		# Resize window by a small amount.
bind CM-j unbind
bind CM-k unbind
bind CM-l unbind
bind CMS-h unbind	# Resize window by a large amount; see cwmrc(5).
bind CMS-j unbind
bind CMS-k unbind
bind CMS-l unbind
bind M-question unbind	# Spawn ``exec program'' dialog.
bind 4-d exec
bind M-period unbind	# Spawn ``ssh to'' dialog.  This parses
bind 4-period ssh
